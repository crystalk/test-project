hi Jeremy!

Hello!

Now, we are in Jeremy's branch.

Another change
Branching

Just kidding, now we are on my branch
Global setup:

 Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email crystalkempen@gmail.com
        

Next steps:

  mkdir test-project
  cd test-project
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:crystalk/test-project.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:crystalk/test-project.git
  git push origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue

